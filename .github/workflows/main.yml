name: Keep n8n Alive

on:
  schedule:
 # 每天 UTC 時間 00:00 執行一次
    - cron: '0 0 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n Space
        run: curl -s https://abaoz1102-n8n-free.hf.space/ || echo "pong"
